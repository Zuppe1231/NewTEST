# よくある質問（FAQ）

WEB会議録音・文字起こしプログラムに関するよくある質問と回答をまとめました。

## インストールと設定

### Q1: Pythonのどのバージョンが必要ですか？

**A:** Python 3.8以上が必要です。Python 3.10または3.11の使用を推奨します。

```cmd
python --version
```

で確認できます。

---

### Q2: インストールに時間がかかりすぎます

**A:** 初回インストールでは、PyTorchやWhisperモデルなど、大きなパッケージをダウンロードするため、10〜30分かかることがあります。これは正常な動作です。

高速化のヒント:
- 安定したインターネット接続を使用
- ファイアウォールやセキュリティソフトウェアを一時的に無効化
- 小さいモデル（tiny、base）から試す

---

### Q3: GPU（NVIDIA）を使用できますか？

**A:** はい、NVIDIA GPUをお持ちの場合、より高速な処理が可能です。

```cmd
# CUDA版PyTorchのインストール
pip uninstall torch torchaudio
pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118
```

**要件:**
- NVIDIA GPU（GTX 1060以上推奨）
- CUDA対応ドライバー
- 4GB以上のGPUメモリ

---

## 録音関連

### Q4: システムオーディオ（PC内部の音声）を録音できますか？

**A:** はい、以下の方法があります：

**方法1: ステレオミックス（Windows標準）**
1. サウンド設定 → サウンドコントロールパネル
2. 録音タブで「ステレオミックス」を有効化

**方法2: 仮想オーディオケーブル（推奨）**
- [VB-CABLE](https://vb-audio.com/Cable/)をインストール
- より柔軟な音声ルーティングが可能

詳細は`SETUP_WINDOWS.md`を参照してください。

---

### Q5: ZoomやTeamsの会議を録音できますか？

**A:** はい、可能です。ただし、以下の点に注意してください：

1. **法的・倫理的配慮:**
   - 参加者全員の同意を得る
   - 会議の録音が許可されているか確認

2. **技術的設定:**
   - システムオーディオとマイクの両方を録音
   - ステレオミックスまたは仮想オーディオケーブルを使用

3. **WEB会議ツールの録音機能と併用:**
   - ZoomやTeamsの録音機能も活用
   - バックアップとして本プログラムを使用

---

### Q6: 録音が途中で止まってしまいます

**A:** 以下を確認してください：

1. **ディスク容量:**
   - 十分な空き容量があるか確認
   - 1時間の録音で約100〜500MBが必要

2. **電源設定:**
   - スリープやスタンバイを無効化
   - 電源プランを「高パフォーマンス」に設定

3. **バックグラウンドアプリ:**
   - 他のアプリケーションを終了
   - ウイルススキャンを一時停止

---

### Q7: マイクの音が小さい/大きい

**A:** Windows側で調整できます：

1. サウンド設定 → デバイスのプロパティ
2. 「音量」スライダーで調整
3. 「追加のデバイスのプロパティ」→「レベル」タブでマイクブーストを調整

プログラム側では現在、録音レベルの調整機能はありません（将来のアップデートで追加予定）。

---

## 文字起こし関連

### Q8: どのWhisperモデルを使うべきですか？

**A:** 用途に応じて選択してください：

| 用途 | 推奨モデル | 理由 |
|------|-----------|------|
| クイックテスト | tiny | 最速、軽量 |
| 日常的な使用 | base | バランスが良い（デフォルト） |
| 重要な会議 | small/medium | 高精度 |
| 専門的な内容 | medium/large | 最高精度、専門用語に強い |
| GPU搭載PC | medium/large | GPU活用で高速処理 |

---

### Q9: 文字起こしの精度が低いです

**A:** 以下の改善方法を試してください：

1. **より大きなモデルを使用:**
   ```cmd
   python web_meeting_recorder.py --model medium
   ```

2. **音質の向上:**
   - 高品質なマイクを使用
   - 静かな環境で録音
   - ノイズキャンセリング機能を活用

3. **正しい言語を指定:**
   ```cmd
   python web_meeting_recorder.py --language ja  # 日本語
   python web_meeting_recorder.py --language en  # 英語
   ```

4. **音声録音設定の最適化:**
   ```cmd
   python web_meeting_recorder.py --sample-rate 44100 --channels 2
   ```

---

### Q10: 英語と日本語が混在した会議はどうすればいいですか？

**A:** Whisperは自動的に言語を検出できます：

```cmd
# 言語を指定しない（自動検出）
python web_meeting_recorder.py --model base
```

または、主要な言語を指定すると精度が向上します：

```cmd
# 日本語メインの場合
python web_meeting_recorder.py --language ja --model small
```

---

### Q11: 専門用語が正しく認識されません

**A:** 以下の対策があります：

1. **より大きなモデルを使用:**
   - mediumまたはlargeモデルは専門用語に強い

2. **後処理で修正:**
   - 生成されたマークダウンファイルを手動で編集
   - 一般的な誤認識パターンは置換で修正

3. **将来のアップデート:**
   - カスタム辞書機能の追加を検討中

---

## トラブルシューティング

### Q12: `ModuleNotFoundError`エラーが出ます

**A:** 依存パッケージがインストールされていません：

```cmd
# 仮想環境を有効化
venv\Scripts\activate

# パッケージを再インストール
pip install -r requirements.txt
```

---

### Q13: `OSError: [Errno -9996] Invalid input device`

**A:** 音声デバイスの問題です：

1. **デバイス一覧を確認:**
   ```cmd
   python web_meeting_recorder.py --list-devices
   ```

2. **特定のデバイスを指定:**
   ```cmd
   python web_meeting_recorder.py --device 1
   ```

3. **他のアプリを確認:**
   - 他のアプリケーションがマイクを使用していないか確認

---

### Q14: メモリ不足エラーが発生します

**A:** 以下の対策を試してください：

1. **小さいモデルを使用:**
   ```cmd
   python web_meeting_recorder.py --model tiny
   ```

2. **他のアプリケーションを終了:**
   - ブラウザ、Office製品などを閉じる

3. **長時間の録音を分割:**
   - 1時間ごとに分けて録音

4. **システムメモリを増設:**
   - 8GB以上のRAMを推奨

---

### Q15: Whisperモデルがダウンロードされません

**A:** ネットワークまたは権限の問題です：

1. **インターネット接続を確認**

2. **ファイアウォール設定:**
   - Pythonの通信を許可

3. **プロキシ設定:**
   ```cmd
   set HTTP_PROXY=http://proxy.example.com:8080
   set HTTPS_PROXY=http://proxy.example.com:8080
   ```

4. **手動ダウンロード:**
   - [Whisper GitHubリリース](https://github.com/openai/whisper/releases)からモデルをダウンロード
   - `~/.cache/whisper/`に配置

---

## 使い方・機能

### Q16: 既存の音声ファイルを文字起こしできますか？

**A:** はい、WAV、MP3、M4Aなど多くの形式に対応しています：

```cmd
python web_meeting_recorder.py --audio-file meeting.mp3 --language ja
```

---

### Q17: 長時間の会議を録音できますか？

**A:** はい、可能です：

```cmd
# 2時間（7200秒）の録音
python web_meeting_recorder.py --duration 7200

# 時間制限なし（手動停止）
python web_meeting_recorder.py
```

**注意:**
- 十分なディスク容量を確保（1時間で約100〜500MB）
- 電源設定でスリープを無効化

---

### Q18: 出力ファイルはどこに保存されますか？

**A:** デフォルトでは`recordings/`フォルダに保存されます：

```
recordings/
  ├── meeting_20260207_143000.wav        # 音声ファイル
  └── transcript_20260207_143000.md      # トランスクリプト
```

保存先を変更する場合：

```cmd
python web_meeting_recorder.py --output-dir my_meetings
```

---

### Q19: トランスクリプトのフォーマットをカスタマイズできますか？

**A:** 現在、マークダウン形式のみ対応していますが、以下の内容が含まれます：

- 全文
- タイムスタンプ付きセグメント
- メタデータ（言語、総時間、セグメント数）

将来のアップデートで、JSON、TXT、SRTなどの形式にも対応予定です。

---

### Q20: プログラムをスケジュール実行できますか？

**A:** はい、Windowsタスクスケジューラーで可能です：

1. タスクスケジューラーを開く
2. 「基本タスクの作成」
3. トリガー（開始時刻）を設定
4. 操作: プログラムの開始
5. プログラム: `python`
6. 引数: `C:\path\to\web_meeting_recorder.py --duration 3600`

---

## パフォーマンス

### Q21: 処理速度を向上させる方法は？

**A:** 以下の方法があります：

1. **GPU（NVIDIA）を使用:**
   - CUDA版PyTorchをインストール
   - 処理速度が2〜10倍向上

2. **小さいモデルを使用:**
   - tinyまたはbaseモデル

3. **サンプリングレートを下げる:**
   ```cmd
   python web_meeting_recorder.py --sample-rate 16000
   ```

4. **バックグラウンドアプリを終了:**
   - CPUとメモリを解放

---

### Q22: 1時間の録音にどのくらいの処理時間がかかりますか？

**A:** 環境とモデルによって異なります：

| 環境 | モデル | 処理時間（目安） |
|------|--------|-----------------|
| CPU（Core i5） | tiny | 5〜10分 |
| CPU（Core i5） | base | 10〜20分 |
| CPU（Core i7） | small | 15〜30分 |
| GPU（RTX 3060） | base | 2〜5分 |
| GPU（RTX 3060） | medium | 5〜10分 |

---

## セキュリティとプライバシー

### Q23: 録音データは外部に送信されますか？

**A:** いいえ、すべての処理はローカルで行われます。

- 音声データは外部に送信されません
- Whisperモデルはローカルで実行
- インターネット接続が必要なのは、初回のモデルダウンロードのみ

---

### Q24: 会議を録音する際の法的注意点は？

**A:** 以下の点に注意してください：

1. **同意の取得:**
   - 参加者全員から録音の同意を得る
   - 会議の冒頭で録音していることを通知

2. **プライバシー保護:**
   - 録音データを適切に管理
   - 不要になったら速やかに削除

3. **企業ポリシー:**
   - 会社の録音ポリシーを確認
   - コンプライアンス部門に相談

4. **著作権:**
   - 会議内容の著作権を尊重
   - 無断での公開や配布は避ける

---

## その他

### Q25: 商用利用は可能ですか？

**A:** このプログラム自体は教育・個人利用を目的としていますが、使用するライブラリ（特にOpenAI Whisper）のライセンスを確認してください。

- **Whisper:** MITライセンス（商用利用可能）
- **PyTorch:** BSDライセンス（商用利用可能）
- **sounddevice:** MITライセンス（商用利用可能）

---

### Q26: 不具合や要望はどこに報告すればいいですか？

**A:** GitHubのIssuesで報告してください：

報告時に以下の情報を含めると、迅速な対応が可能です：

- Windowsのバージョン
- Pythonのバージョン
- エラーメッセージの全文
- 実行したコマンド
- 期待される動作と実際の動作

---

### Q27: このプログラムの将来の機能は？

**A:** 以下の機能を検討中です：

- 話者分離（誰が話しているかを識別）
- リアルタイム文字起こし
- 複数の出力形式（JSON、SRT、VTT）
- カスタム辞書機能
- GUI版
- クラウド統合（GoogleDrive、OneDrive）

ご要望があればGitHub Issuesでお知らせください！

---

## 関連情報

- [README.md](README.md) - 基本的な使い方
- [SETUP_WINDOWS.md](SETUP_WINDOWS.md) - Windows環境の詳細セットアップ
- [example_usage.py](example_usage.py) - 使用例のコード

問題が解決しない場合は、GitHubのIssuesでお気軽にご質問ください。
