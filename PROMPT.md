# WEB会議 全音声録音 Pythonプログラム作成プロンプト

## プロンプト本文

```
あなたは経験豊富なPythonエンジニアです。
Windows PCでWEB会議（Zoom, Microsoft Teams, Google Meet等）を行う際に、
**すべての音声**を録音するPythonプログラムを作成してください。

## 要件

### 基本機能
1. **マイク音声の録音**: ユーザー自身の発話（マイク入力）を録音する
2. **システム音声の録音**: 相手の発話やWEB会議アプリから出力される音声（スピーカー出力）を録音する
3. **音声のミキシング**: マイク音声とシステム音声を1つのファイルにミキシングして保存する
4. **リアルタイム録音**: WEB会議中にリアルタイムで録音を行い、会議終了後にファイルを保存する

### 技術要件
- **OS**: Windows 10 / 11
- **音声キャプチャ方式**: Windows WASAPI（Windows Audio Session API）のループバックモードを使用してシステム音声をキャプチャする
- **マイク入力**: 標準のオーディオ入力デバイスを使用する
- **出力形式**: WAVファイル（非圧縮）をデフォルトとし、オプションでMP3変換も可能にする
- **サンプルレート**: 44100Hz または 48000Hz
- **チャンネル**: モノラルまたはステレオ（設定可能）

### UIとユーザー体験
- **コンソールUI（CUI）**: シンプルなコマンドラインインターフェース
- **操作方法**:
  - プログラムを起動するとデバイス一覧が表示される
  - マイクデバイスとスピーカーデバイスを選択できる
  - Enterキーで録音開始
  - Enterキーまたは Ctrl+C で録音停止
- **録音中の表示**: 経過時間と音量レベル（VUメーター風）をリアルタイムで表示する
- **ファイル名**: 録音開始日時を含む自動命名（例: `recording_2026-02-08_14-30-00.wav`）

### エラーハンドリング
- オーディオデバイスが見つからない場合のエラー処理
- 録音中のデバイス切断への対処
- ディスク容量不足の警告
- WASAPIループバックが利用できない場合のフォールバック処理

### 依存ライブラリ
- `sounddevice`: オーディオ入出力（WASAPIサポート）
- `soundfile`: WAVファイルの書き込み
- `numpy`: 音声データの処理・ミキシング
- `pydub`（オプション）: MP3変換用

### プログラム構成
1. **AudioRecorder クラス**: メインの録音ロジックを管理
   - デバイスの列挙・選択
   - マイク音声のキャプチャ（InputStream）
   - システム音声のキャプチャ（WASAPI Loopbackを利用したInputStream）
   - 2つの音声ストリームのミキシング
   - ファイルへの書き出し
2. **main() 関数**: CLIインターフェースとユーザー操作のハンドリング
3. **設定**: コマンドライン引数またはconfig.jsonで設定可能にする
   - 出力ディレクトリ
   - サンプルレート
   - 出力形式（WAV/MP3）
   - バッファサイズ

### 補足事項
- WASAPIループバックはWindowsでのみ利用可能。macOS/Linuxでの動作は保証しない
- 管理者権限は不要（WASAPIループバックは一般ユーザー権限で利用可能）
- 長時間録音（数時間）にも対応できるよう、メモリ効率の良い実装にする（ストリーミング書き込み）
- スレッドセーフな実装にする（マイクとシステム音声は別スレッドでキャプチャ）
```

## プロンプト設計のポイント解説

### 1. なぜ「すべての音声」が重要か
WEB会議の録音では、単にマイクだけを録音しても相手の音声が含まれません。
Windows WASAPIのループバック機能を使うことで、スピーカーから出力される
相手の音声もキャプチャできます。これにより、会議の全体像を録音できます。

### 2. WASAPI ループバックとは
Windowsのオーディオアーキテクチャ（WASAPI）には「ループバック」モードがあり、
スピーカーに出力される音声をそのままキャプチャできます。
これは仮想オーディオデバイス等の追加ソフトウェアなしで利用できるため、
最もシンプルで信頼性の高い方法です。

### 3. sounddevice ライブラリの選択理由
- PyAudioよりも現代的で使いやすいAPI
- WASAPIのループバックモードに対応（`wasapi_exclusive` パラメータ等）
- NumPyとの統合が優秀
- コールバックベースの非同期録音が可能

### 4. ミキシングのアプローチ
マイク音声とシステム音声は別々のストリームでキャプチャし、
NumPyで加算（ミキシング）してから1つのファイルに書き出します。
サンプルレートが異なる場合はリサンプリングが必要です。

### 5. メモリ効率
長時間の会議に対応するため、音声データをすべてメモリに保持するのではなく、
チャンクごとにファイルに書き出す「ストリーミング書き込み」方式を採用します。
