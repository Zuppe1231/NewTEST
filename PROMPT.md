# Windows PC WEB会議音声録音プログラム作成プロンプト

## 概要
Windows PCでWEB会議を行う際に、システムの全ての音声（マイク入力とスピーカー出力の両方）を録音するPythonプログラムを作成してください。

## 要件

### 機能要件
1. **音声キャプチャ**
   - システムのスピーカー出力（相手の声、通知音など）を録音
   - マイク入力（自分の声）を録音
   - 両方の音声をミックスして1つのファイルに保存

2. **録音制御**
   - 録音の開始・停止をコマンドまたはGUIで制御
   - 録音中の状態表示（録音時間、ファイルサイズなど）
   - 緊急停止機能（Ctrl+Cやストップボタン）

3. **ファイル保存**
   - WAV形式で保存（高音質、編集しやすい）
   - MP3形式のオプション（ファイルサイズ削減）
   - ファイル名に日時を自動付与（例: recording_2026-02-08_14-30-00.wav）
   - 保存先フォルダの指定機能

4. **設定機能**
   - サンプリングレート設定（44.1kHz、48kHzなど）
   - ビット深度設定（16bit、24bitなど）
   - チャンネル数設定（モノラル/ステレオ）
   - 音声品質プリセット（低/中/高）

### 技術要件
1. **使用ライブラリ**
   - `pyaudio` または `sounddevice`: 音声入力
   - `wave`: WAVファイル保存
   - `pydub` (オプション): MP3変換
   - `PyQt5` または `tkinter` (オプション): GUI
   - `pywin32` または `comtypes`: Windowsオーディオシステムアクセス

2. **Windows固有の機能**
   - WASAPI (Windows Audio Session API) を使用してループバック録音を実装
   - システムオーディオデバイスの列挙と選択
   - 仮想オーディオデバイスのサポート（VB-CABLEなど）

3. **エラー処理**
   - オーディオデバイスが見つからない場合の処理
   - ディスク容量不足の検出
   - 録音中の例外処理（デバイス切断など）
   - ログファイルの生成

### 非機能要件
1. **パフォーマンス**
   - CPU使用率を10%以下に抑える
   - メモリ使用量を効率化（バッファリング）
   - 長時間録音（4時間以上）に対応

2. **ユーザビリティ**
   - 初心者でも使いやすいインターフェース
   - 設定ファイル（config.ini または config.json）でデフォルト設定を保存
   - README.mdに詳細な使用方法を記載

3. **互換性**
   - Windows 10/11 対応
   - Python 3.8以上

## 実装ガイドライン

### 推奨アーキテクチャ
```
audio_recorder/
├── main.py                 # エントリーポイント
├── recorder.py            # 録音エンジン
├── audio_device.py        # オーディオデバイス管理
├── file_manager.py        # ファイル保存処理
├── config.py              # 設定管理
├── gui.py                 # GUI (オプション)
├── requirements.txt       # 依存パッケージ
├── config.ini             # デフォルト設定
└── README.md              # ドキュメント
```

### コア機能の実装手順
1. **WASAPI録音の実装**
   - Windowsのループバックデバイスを検出
   - pyaudioまたはsounddeviceでループバック録音を設定
   - リアルタイムオーディオストリームの処理

2. **マイクとスピーカーのミキシング**
   - 2つの音声ストリームを同時にキャプチャ
   - numpy配列で音声データをミックス
   - 音量バランスの調整機能

3. **ファイル保存の最適化**
   - バッファリングを使用してリアルタイム書き込み
   - チャンク単位での書き込みでメモリ効率化
   - 録音完了時のファイル整合性チェック

### セキュリティとプライバシー
1. 録音開始時に明確な通知（ビープ音やビジュアル表示）
2. 暗号化オプション（AES-256）
3. 自動削除機能（指定日数後）
4. パスワード保護機能

## テスト計画
1. **単体テスト**
   - オーディオデバイス検出
   - ファイル保存・読み込み
   - 設定の読み書き

2. **統合テスト**
   - 実際のWEB会議ツール（Zoom、Teams、Google Meetなど）での動作確認
   - 長時間録音のストレステスト
   - 各種音声フォーマットでの保存確認

3. **ユーザビリティテスト**
   - 初心者による操作テスト
   - エラーメッセージの分かりやすさ

## ドキュメント要件
1. **README.md**
   - インストール手順（pipコマンド、依存関係）
   - 基本的な使用方法（CLIとGUI）
   - トラブルシューティング
   - FAQ
   - ライセンス情報

2. **コード内ドキュメント**
   - 関数・クラスのdocstring（Google形式またはNumPy形式）
   - 複雑なロジックへのインラインコメント
   - 型ヒントの使用（Python 3.8+）

## オプション機能（拡張）
1. クラウドストレージへの自動アップロード（Google Drive、Dropbox）
2. 音声の自動文字起こし（Whisper APIなど）
3. ノイズリダクション機能
4. 複数の会議を自動検出して個別ファイルに保存
5. スケジュール録音（指定時刻に自動開始・停止）
6. リアルタイム音声レベルメーター
7. 無音区間の自動カット

## 法的・倫理的注意事項
- 録音する前に参加者全員の同意を得ること
- 地域の法律（盗聴法など）を遵守すること
- プログラムに免責事項を明記すること

## サンプル実行例
```bash
# 基本的な使用方法
python main.py --start

# カスタム設定での録音
python main.py --start --output ./recordings --format mp3 --quality high

# GUIモードで起動
python main.py --gui

# デバイス一覧表示
python main.py --list-devices
```

## 期待される成果物
1. 動作するPythonプログラム（全ソースコード）
2. requirements.txt（依存パッケージリスト）
3. README.md（詳細なドキュメント）
4. config.iniのサンプル
5. 簡単な使用デモ動画またはスクリーンショット（オプション）

このプロンプトに従って、実用的で高品質なWindows PC用WEB会議音声録音プログラムを開発してください。
